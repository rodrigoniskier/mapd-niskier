(()=>{const box=document.querySelector('[data-carousel]');if(!box)return;const slides=[...box.querySelectorAll('[data-slide]')],prev=box.querySelector('[data-carousel-prev]'),next=box.querySelector('[data-carousel-next]'),bar=box.querySelector('[data-carousel-progress]'),finish=document.querySelector('[data-finish-review]');let i=0;function draw(){slides.forEach((s,n)=>s.classList.toggle('active',n===i));bar.style.width=`${((i+1)/slides.length)*100}%`;prev.disabled=i===0;next.textContent=i===slides.length-1?'Finalizar revisão':'Próxima';finish.hidden=i!==slides.length-1}prev?.addEventListener('click',()=>{if(i>0)i--;draw()});next?.addEventListener('click',()=>{if(i<slides.length-1)i++;draw()});draw()})();
